// Public runtime config for the static frontend.
// Only PUBLIC values live here (marketing URLs). Secrets stay in server env vars only.
window.RR_CONFIG = {
  kofiUrl: "https://ko-fi.com/johnwakaba",
  priceLabel: "$19 one-time",
};
